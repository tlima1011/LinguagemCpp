#include <bits/stdc++.h>

using namespace std;

int main()
{
    int subtracao, soma = 0, x, y;
    cout << "Digite o valor de X: ";
    cin >> x;
    cout << "Digite o valor de Y: ";
    cin >> y;
    soma = x + y;
    subtracao = x - y;

    cout << "SOMA = " << soma << endl;
    cout << "SUBTRACAO = " << subtracao << endl;
    return 0;
}
